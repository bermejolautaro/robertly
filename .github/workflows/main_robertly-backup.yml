# # Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# # More GitHub Actions for Azure: https://github.com/Azure/actions

# name: Build and deploy ASP.Net Core app to Azure Web App - robertly

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# env:
#   AZURE_WEBAPP_NAME: robertly
#   AZURE_WEBAPP_PACKAGE_PATH: "./publish"

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up .NET Core
#         uses: actions/setup-dotnet@v1
#         with:
#           dotnet-version: '6.0.x'
#           # include-prerelease: true

#       - name: Build with dotnet
#         run: dotnet build ./robertly-net-api/robertly-net-api.sln --configuration Release

#       - name: dotnet publish
#         run: dotnet publish ./robertly-net-api/robertly-net-api.sln -c Release -o '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}'

#       - name: Deploy to Azure Web App
#         id: deploy-to-webapp
#         uses: azure/webapps-deploy@v2
#         with:
#           app-name: ${{ env.AZURE_WEBAPP_NAME }}
#           package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
#           publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}

#       # - name: Upload artifact for deployment job
#       #   uses: actions/upload-artifact@v3
#       #   with:
#       #     name: .net-app
#       #     path: ${{env.DOTNET_ROOT}}/myapp

#   # deploy:
#   #   runs-on: ubuntu-latest
#   #   needs: build
#   #   environment:
#   #     name: 'Production'
#   #     url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

#   #   steps:
#   #     - name: Download artifact from build job
#   #       uses: actions/download-artifact@v3
#   #       with:
#   #         name: .net-app

#   #     - name: Deploy to Azure Web App
#   #       id: deploy-to-webapp
#   #       uses: azure/webapps-deploy@v2
#   #       with:
#   #         app-name: ${{ AZURE_WEBAPP_NAME }}
#   #         package: ${{ AZURE_WEBAPP_PACKAGE_PATH }}
#   #         publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}

#   # deploy:
#   #   runs-on: windows-latest
#   #   needs: build
#   #   environment:
#   #     name: 'Production'
#   #     url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

#   #   steps:
#   #     - name: Download artifact from build job
#   #       uses: actions/download-artifact@v3
#   #       with:
#   #         name: .net-app

#   #     - name: Deploy to Azure Web App
#   #       id: deploy-to-webapp
#   #       uses: azure/webapps-deploy@v2
#   #       with:
#   #         app-name: 'robertly'
#   #         slot-name: 'Production'
#   #         package: .
#   #         publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_2F5821C575B949918E83500E869CD964 }}
